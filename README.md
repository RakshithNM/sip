# StepUp

A frontend-only SIP step-up calculator built with Vue 3, TypeScript, Vite, and modern CSS. It compares a fixed monthly SIP with a strategy that redirects 70% of every annual salary increase into the SIP.

## Development

```sh
npm install
npm run dev
```

## Checks

```sh
npm run typecheck
npm run build
```
